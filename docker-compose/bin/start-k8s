#!/bin/bash
set -e

source bin/source

kubectx docker-desktop

kubectl apply -k ../kubernetes/provenance-only-local

sleep 5 # allow creation of docker network that will need to be linked

docker-compose up -d
